diff --git a/app/src/main/java/com/quaderno/sudoku/MainActivity.kt b/app/src/main/java/com/quaderno/sudoku/MainActivity.kt
--- a/app/src/main/java/com/quaderno/sudoku/MainActivity.kt
+++ b/app/src/main/java/com/quaderno/sudoku/MainActivity.kt
@@
     if (showExitDialog) {
         AlertDialog(
             onDismissRequest = { showExitDialog = false },
-            title = { Text("Partita in corso", fontWeight = FontWeight.Bold) },
-            text = { Text("Cosa vuoi fare?") },
+            title = { Text("Sei sicuro di uscire?", fontWeight = FontWeight.Bold) },
             confirmButton = {
-                TextButton(onClick = { showExitDialog = false }) { Text("Riprendi") }
+                Button(
+                    onClick = {
+                        saveResumeGame()
+                        showExitDialog = false
+                        screen = if (dailyGameDate != null) AppScreen.DAILY else AppScreen.HOME
+                    },
+                    colors = ButtonDefaults.buttonColors(containerColor = AppBlue)
+                ) { Text("Sì", color = Color.White, fontWeight = FontWeight.Bold) }
             },
             dismissButton = {
-                Column(horizontalAlignment = Alignment.End) {
-                    TextButton(onClick = {
-                        stats.abandonActive()
-                        statsVersion++
-                        game.reset(game.difficulty)
-                        clearResumeGame()
-                        dailyGameDate = null
-                        showExitDialog = false
-                        screen = AppScreen.HOME
-                    }) { Text("Elimina") }
-                    TextButton(onClick = {
-                        saveResumeGame()
-                        showExitDialog = false
-                        screen = if (dailyGameDate != null) AppScreen.DAILY else AppScreen.HOME
-                    }) { Text("Torna indietro") }
-                }
+                OutlinedButton(onClick = { showExitDialog = false }) {
+                    Text("No", fontWeight = FontWeight.Bold)
+                }
             }
         )
     }
@@
         OutlinedButton(
             onClick = onResume,
             enabled = hasResume,
             modifier = Modifier.fillMaxWidth().height(50.dp),
             shape = RoundedCornerShape(12.dp),
             colors = ButtonDefaults.outlinedButtonColors(
-                contentColor = AppBlue,
+                containerColor = if (hasResume) Color(0xFF356DB9) else Color.Transparent,
+                contentColor = if (hasResume) Color.White else AppBlue,
                 disabledContentColor = AppText.copy(alpha = 0.45f)
             )
         ) {
diff --git a/app/build.gradle.kts b/app/build.gradle.kts
--- a/app/build.gradle.kts
+++ b/app/build.gradle.kts
@@
-        versionCode = 36
-        versionName = "1.35"
+        versionCode = 37
+        versionName = "1.36"
